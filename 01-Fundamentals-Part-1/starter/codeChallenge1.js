/*
//*CALCULATE THE BODY MASS INDEX
const calculateBMI =(mass,height)=>{
    const result = mass / height**2;
    return result;
}

const compareTwoBMI=(bmi1,bmi2,name1,name2)=>{
    alert(`RESULTS: \n Mark = ${bmi1.toFixed(2)} \n John = ${bmi2.toFixed(2)}`);
    if(bmi1 > bmi2){
        alert(`${name1} it's bigger than ${name2}`);
    }else{
        alert(`${name2} it's bigger than ${name1}`);
    }
}

const massMark = 78;
const heightMark = 1.69;

const massJohn = 92;
const heightJohn = 1.95;

const markResult = calculateBMI(massMark,heightMark);
const johnResult = calculateBMI(massJohn,heightJohn);

compareTwoBMI(markResult,johnResult,'Mark',"John");
*/
