//*BMI = weight/height**2
/*
const mark ={
    name: 'Mark Miller',
    mass: 78,
    height: 1.69,
    calculateBMI : function(){return this.mass / this.height**2},
};
const john ={
    name: 'john Smith',
    mass: 92,
    height: 1.95,
    calculateBMI : function(){return this.mass / this.height**2},
};

const markInformation = mark.calculateBMI();
console.log(markInformation)
const johnInformation = john.calculateBMI();
console.log(johnInformation)

markInformation > johnInformation ? console.log(`The winner is ${mark.name}\nMass -> ${mark.mass}\nHeight -> ${mark.height}\nBMI -> ${markInformation}`) : console.log(`The winner is ${john.name}\nMass -> ${mark.mass}\nHeight -> ${mark.height}\nBMI -> ${johnInformation}`);
*/